input:
  schema:
    properties:
      action_type:
        enum:
        - define_workflow
        - coordinate_agents
        - dispatch_task
        - manage_state
output:
  schema:
    properties:
      agent_assignments:
        type: array
      execution_steps:
        type: array
      state_transitions:
        type: array
      workflow_plan:
        type: object
skeleton_id: nucleus-orchestrator
version: 1.0.0
