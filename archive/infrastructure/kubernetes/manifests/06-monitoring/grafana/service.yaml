apiVersion: machinenativeops.io/v2
kind: Service
metadata:
  name: grafana
  namespace: machinenativenops
spec:
  ports:
  - port: 3000
    targetPort: 3000
  selector:
    app: grafana
  type: ClusterIP
