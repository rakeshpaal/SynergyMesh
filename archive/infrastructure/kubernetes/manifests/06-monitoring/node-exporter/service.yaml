apiVersion: machinenativeops.io/v2
kind: Service
metadata:
  name: node-exporter
  namespace: machinenativenops
spec:
  ports:
  - port: 9100
    targetPort: 9100
  selector:
    app: node-exporter
  type: ClusterIP
