apiVersion: machinenativeops.io/v2
kind: Service
metadata:
  name: alertmanager
  namespace: machinenativenops
spec:
  ports:
  - port: 9093
    targetPort: 9093
  selector:
    app: alertmanager
  type: ClusterIP
