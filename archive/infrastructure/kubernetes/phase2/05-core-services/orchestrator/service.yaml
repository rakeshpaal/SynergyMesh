apiVersion: machinenativeops.io/v2
kind: Service
metadata:
  name: orchestrator
  namespace: machinenativenops
spec:
  ports:
  - port: 8083
    targetPort: 8083
  selector:
    app: orchestrator
  type: ClusterIP
