apiVersion: machinenativeops.io/v2
kind: Service
metadata:
  name: postgres-service
  namespace: machinenativenops
spec:
  clusterIP: None
  ports:
  - name: postgres
    port: 5432
    targetPort: 5432
  selector:
    app: postgres
