apiVersion: machinenativeops.io/v2
kind: Service
metadata:
  annotations:
    machinenativeops.io/description: Advanced system frontend service
  labels:
    machinenativeops.io/app: advanced-system
    machinenativeops.io/environment: production
    machinenativeops.io/lifecycle: active
    machinenativeops.io/team: frontend-team
  name: advanced-system
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: http
  selector:
    app: advanced-system
  sessionAffinity: None
  type: ClusterIP
