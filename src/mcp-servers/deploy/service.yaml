apiVersion: machinenativeops.io/v2
kind: Service
metadata:
  annotations:
    machinenativeops.io/description: MCP servers service
  labels:
    machinenativeops.io/app: mcp-servers
    machinenativeops.io/environment: production
    machinenativeops.io/lifecycle: active
    machinenativeops.io/team: platform-team
  name: mcp-servers
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: http
  selector:
    app: mcp-servers
  sessionAffinity: None
  type: ClusterIP
