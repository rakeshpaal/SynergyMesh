apiVersion: machinenativeops.io/v2
kind: Service
metadata:
  name: prometheus
  namespace: machinenativenops
spec:
  ports:
  - port: 9090
    targetPort: 9090
  selector:
    app: prometheus
  type: ClusterIP
