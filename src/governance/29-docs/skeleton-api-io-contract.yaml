input:
  schema:
    properties:
      action_type:
        enum:
        - design_api
        - version_api
        - deprecate_api
        - validate_contract
output:
  schema:
    properties:
      api_spec:
        format: openapi
        type: string
      compatibility_report:
        type: object
      versioning_strategy:
        type: object
skeleton_id: api-governance
version: 1.0.0
