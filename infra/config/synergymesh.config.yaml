# ═══════════════════════════════════════════════════════════════════════════════
#                    SynergyMesh Configuration
#                    無人化自主協同網格系統配置
# ═══════════════════════════════════════════════════════════════════════════════
#
# This is the main configuration file for the SynergyMesh system.
# It integrates all 13 phases into a unified automation system.
#
# 這是 SynergyMesh 系統的主配置文件。
# 它將所有 13 個階段整合成統一的自動化系統。
#
# ═══════════════════════════════════════════════════════════════════════════════

# System Metadata
system:
  name: SynergyMesh
  version: "1.0.0"
  description: "無人化自主協同網格系統 - Autonomous Coordination Grid System"
  environment: development # development, staging, production

# Logging Configuration
logging:
  level: INFO # DEBUG, INFO, WARNING, ERROR, CRITICAL
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
  output:
    - console
    - file
  file_path: logs/synergymesh.log
  rotation:
    max_size_mb: 100
    backup_count: 5

# ═══════════════════════════════════════════════════════════════════════════════
#                         Phase Configurations
# ═══════════════════════════════════════════════════════════════════════════════

phases:
  # Phase 1: Core Autonomous Coordination (自主協調核心)
  phase_1:
    enabled: true
    name: "Core Autonomous Coordination"
    description: "自主協調核心"
    components:
      - NaturalLanguageProcessor
      - AutonomousCoordinator
      - SelfEvolutionEngine
      - EcosystemOrchestrator
    config:
      auto_evolution: true
      coordination_interval_seconds: 60

  # Phase 2: Advanced Interaction & Orchestration (進階互動與編排)
  phase_2:
    enabled: true
    name: "Advanced Interaction & Orchestration"
    description: "進階互動與編排"
    components:
      - NaturalLanguageInteractionLayer
      - IntentUnderstandingEngine
      - TaskOrchestrationEngine
    config:
      multi_modal: true
      intent_confidence_threshold: 0.8

  # Phase 3: AI Core, Bridges & Automation (AI核心、橋接與自動化)
  phase_3:
    enabled: true
    name: "AI Core, Bridges & Automation"
    description: "AI核心、橋接與自動化"
    components:
      - AIDecisionEngine
      - LanguageBridgeManager
      - ZeroTouchDeploymentEngine
    config:
      decision_confidence_threshold: 0.85
      supported_languages:
        - Python
        - JavaScript
        - TypeScript
        - Go
        - Rust
        - Java
        - C#
      deployment_strategies:
        - Rolling
        - BlueGreen
        - Canary
        - Recreate

  # Phase 4: Autonomous Trust & Governance (自主信任與治理)
  phase_4:
    enabled: true
    name: "Autonomous Trust & Governance"
    description: "自主信任與治理"
    components:
      - AutonomousTrustEngine
      - AutoGovernanceHub
    config:
      trust_score_threshold: 0.7
      auto_approval_threshold: 0.9
      require_human_approval: false

  # Phase 5: AI Quality & Bug Prevention (AI品質與漏洞預防)
  phase_5:
    enabled: true
    name: "AI Quality & Bug Prevention"
    description: "AI品質與漏洞預防"
    components:
      - HallucinationDetector
      - ContextUnderstandingEngine
      - AutoBugDetector
    config:
      hallucination_threshold: 0.3
      auto_fix_enabled: true
      learning_enabled: true

  # Phase 6: AI Supreme Directive Constitution (AI最高指令憲法)
  phase_6:
    enabled: true
    name: "AI Supreme Directive Constitution"
    description: "AI最高指令憲法 - 三層憲法體系"
    components:
      - ConstitutionEngine
      - PolicyAsPrompt
      - GuardrailSystem
    config:
      enforcement_mode: strict # strict, warn, permissive
      layers:
        - FundamentalLaws # 根本法則
        - OperationalRules # 操作規則
        - AdaptiveGuidelines # 自適應指南

  # Phase 7: Knowledge & Skills Training (知識與技能訓練)
  phase_7:
    enabled: true
    name: "Knowledge & Skills Training"
    description: "知識與技能訓練 + 虛擬專家團隊"
    components:
      - KnowledgeBase
      - SkillsTrainingSystem
      - ExampleLibrary
      - VirtualExpertTeam
    config:
      domains:
        - DATABASE
        - SECURITY
        - ARCHITECTURE
        - PERFORMANCE
      experts:
        - id: expert_alex_chen
          name: "Dr. Alex Chen"
          role: "AI 架構師"
          avatar: "🧠"
        - id: expert_sarah_wong
          name: "Sarah Wong"
          role: "NLP 專家"
          avatar: "💬"
        - id: expert_marcus_johnson
          name: "Marcus Johnson"
          role: "安全架構師"
          avatar: "🔐"
        - id: expert_li_wei
          name: "Li Wei"
          role: "數據庫專家"
          avatar: "🗄️"
        - id: expert_emma_thompson
          name: "Emma Thompson"
          role: "DevOps 專家"
          avatar: "🚀"
        - id: expert_james_miller
          name: "James Miller"
          role: "系統架構師"
          avatar: "🏗️"

  # Phase 8: Execution Engine & Tech Stack (執行引擎與技術棧)
  phase_8:
    enabled: true
    name: "Execution Engine & Tech Stack"
    description: "執行引擎與技術棧"
    components:
      - ExecutionEngine
      - CapabilityRegistry
      - ConnectorManager
      - TechStackConfig
      - FrameworkIntegrations
      - MultiAgentCoordinator
      - PythonBridge
    config:
      tech_stack:
        ai_core: "Python 3.11+"
        orchestration: "TypeScript 5.0+"
      frameworks:
        - LangChain
        - CrewAI
        - AutoGen
        - LangGraph
      max_concurrent_executions: 10

  # Phase 9: Complete Execution Architecture (完整執行架構)
  phase_9:
    enabled: true
    name: "Complete Execution Architecture"
    description: "完整執行架構"
    components:
      - ToolSystem
      - LangChainIntegration
      - AgentOrchestrator
      - FunctionCallingSystem
      - MCPIntegration
    config:
      tool_categories:
        - DATABASE
        - FILESYSTEM
        - API
        - DEPLOYMENT
        - SECURITY
        - MONITORING
        - CODE
        - COMMUNICATION
      mcp_enabled: true

  # Phase 10: Safety Mechanisms (安全機制)
  phase_10:
    enabled: true
    name: "Safety Mechanisms"
    description: "安全機制系統"
    components:
      - CircuitBreaker
      - EscalationLadder
      - RollbackSystem
      - AnomalyDetector
      - EmergencyStop
      - SafetyNet
    config:
      circuit_breaker:
        failure_threshold: 5
        success_threshold: 2
        timeout_seconds: 60
      escalation_levels:
        - NORMAL
        - WARNING
        - ALERT
        - CRITICAL
        - EMERGENCY
        - DISASTER
      safety_layers: 6

  # Phase 11: Intelligent Monitoring & Remediation (智能監控與修復)
  phase_11:
    enabled: true
    name: "Intelligent Monitoring & Remediation"
    description: "智能監控和自動修復系統"
    components:
      - IntelligentMonitoringSystem
      - SmartAnomalyDetector
      - AutoDiagnosisEngine
      - AutoRemediationEngine
      - SelfLearningEngine
      - ObservabilityPlatform
    config:
      monitoring_interval_seconds: 30
      anomaly_detection:
        strategies:
          - STATISTICAL
          - ML
          - THRESHOLD
          - PATTERN
          - HYBRID
      auto_remediation: true
      self_learning: true

  # Phase 12: CI Error Auto-Handler (CI錯誤自動處理)
  phase_12:
    enabled: true
    name: "CI Error Auto-Handler"
    description: "GitHub Issues CI 錯誤自動處理"
    components:
      - CIErrorAnalyzer
      - IssueManager
      - AutoFixEngine
      - FixStatusTracker
    config:
      error_categories:
        - BUILD_ERROR
        - TEST_FAILURE
        - LINT_ERROR
        - TYPE_ERROR
        - DEPENDENCY_ERROR
        - SECURITY_SCAN
        - DEPLOYMENT_ERROR
      auto_fix_enabled: true
      create_issues: true

  # Phase 13: Deep Verifiable YAML System (深度可驗證YAML系統)
  phase_13:
    enabled: true
    name: "Deep Verifiable YAML System"
    description: "深度可驗證 YAML 模組系統"
    components:
      - YAMLModuleDefinition
      - YAMLSchemaValidator
      - PolicyGate
      - CIVerificationPipeline
      - SLSAProvenanceGenerator
      - SBOMGenerator
      - AuditLogger
    config:
      module_lifecycle:
        - DRAFT
        - REVIEW
        - APPROVED
        - ACTIVE
        - DEPRECATED
      policy_categories:
        - SECURITY
        - COMPLIANCE
        - QUALITY
        - PERFORMANCE
        - GOVERNANCE
      slsa_level: 3
      sbom_format: CycloneDX

# ═══════════════════════════════════════════════════════════════════════════════
#                         Integration Settings
# ═══════════════════════════════════════════════════════════════════════════════

integration:
  # GitHub Integration
  github:
    enabled: true
    auto_create_issues: true
    auto_fix_prs: true
    workflows_to_monitor:
      - CI
      - Build
      - Test
      - Deploy
      - Lint

  # MCP Integration
  mcp:
    enabled: true
    expose_tools: true
    consume_external_tools: true

  # Database Connectors
  database:
    supported:
      - PostgreSQL
      - MySQL
      - MongoDB
      - Redis

  # Cloud Providers
  cloud:
    supported:
      - AWS
      - GCP
      - Azure

# ═══════════════════════════════════════════════════════════════════════════════
#                         Automation Settings
# ═══════════════════════════════════════════════════════════════════════════════

automation:
  # Task Pipeline
  pipeline:
    max_concurrent_tasks: 5
    default_timeout_seconds: 300
    enable_retries: true
    max_retries: 3
    priority_queue: true

  # Task Routing
  routing:
    auto_route: true
    routes:
      natural_language: [phase_1, phase_2]
      decision: [phase_3, phase_4]
      quality_check: [phase_5, phase_6]
      training: [phase_7]
      expert_consultation: [phase_7]
      execution: [phase_8, phase_9]
      safety_check: [phase_10]
      monitoring: [phase_11]
      ci_error: [phase_12]
      yaml_validation: [phase_13]

  # Self-Learning
  learning:
    enabled: true
    track_metrics: true
    improve_routing: true

# ═══════════════════════════════════════════════════════════════════════════════
#                         Performance Settings
# ═══════════════════════════════════════════════════════════════════════════════

performance:
  # Resource Limits
  resources:
    max_memory_mb: 4096
    max_cpu_percent: 80
    max_disk_gb: 100

  # Caching
  cache:
    enabled: true
    ttl_seconds: 3600
    max_size_mb: 512

  # Rate Limiting
  rate_limiting:
    enabled: true
    requests_per_minute: 100

# ═══════════════════════════════════════════════════════════════════════════════
#                         Security Settings
# ═══════════════════════════════════════════════════════════════════════════════

security:
  # Authentication
  auth:
    enabled: true
    methods:
      - api_key
      - oauth2

  # Encryption
  encryption:
    at_rest: true
    in_transit: true

  # Audit
  audit:
    enabled: true
    log_all_operations: true
    retention_days: 90
