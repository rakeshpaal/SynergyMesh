# OPA Gatekeeper Constraint for Vision Statement
# Applies the vision validation template
# Phase 3 - OPA Gatekeeper Integration

apiVersion: constraints.gatekeeper.sh/v1beta1
kind: GovernanceVisionStatement
metadata:
  name: vision-validation
  annotations:
    gac-phase: "3-enforcement"
spec:
  match:
    kinds:
      - apiGroups: ["governance.kai"]
        kinds: ["VisionStatement"]
  parameters:
    requireEnglishTranslation: true
