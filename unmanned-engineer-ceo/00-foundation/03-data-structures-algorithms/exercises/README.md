# Exercises: Backend/Distributed Focus

1. **Shard-Aware
   Index**：設計一個同時支援 range 與哈希的索引，實作於 core/storage-engine。
2. **Conflict Resolver**：為最終一致系統撰寫 CRDT 或向量時鐘解決方案。
3. **Streaming Joins**：在 observability pipeline 中實作 window join。
4. **Graph Lineage**：建模 docs/knowledge-graph.yaml 的推導鏈，並提供推理查詢。

> 建議提交 PR 至 `tests/` 目錄並更新此清單的結果鏈接。
