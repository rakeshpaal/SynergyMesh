# 55-slo-sli | Service Level Objectives & Indicators (SLO/SLI)

This placeholder directory anchors the `55-slo-sli` governance dimension so
closed-loop CI checks can verify required execution coverage. A legacy folder
`55-sli-slo` (reversed ordering) exists; this directory name matches the
canonical path declared in `governance/index/dimensions.json`.

# 55-slo-sli | 服務等級目標與指標

此目錄用於標記強制執行的 SLO/SLI 治理維度，確保新治理 CI 的 DAG 與目錄檢查可通過。
