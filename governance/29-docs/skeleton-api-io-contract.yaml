version: "1.0.0"
skeleton_id: "api-governance"

input:
  schema:
    properties:
      action_type:
        enum: ["design_api", "version_api", "deprecate_api", "validate_contract"]

output:
  schema:
    properties:
      api_spec: { type: "string", format: "openapi" }
      versioning_strategy: { type: "object" }
      compatibility_report: { type: "object" }
