apiVersion: v1
kind: LimitRange
metadata:
  name: prod-limit-range
  namespace: chatops-prod
spec:
  limits:
    - default:
        cpu: "1"
        memory: 1Gi
      defaultRequest:
        cpu: 200m
        memory: 256Mi
      max:
        cpu: "2"
        memory: 2Gi
      type: Container
