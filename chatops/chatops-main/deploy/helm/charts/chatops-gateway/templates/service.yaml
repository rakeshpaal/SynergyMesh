apiVersion: v1
kind: Service
metadata:
  name: {{ include "chatops-gateway.fullname" . }}
  labels:
    {{- include "chatops-gateway.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chatops-gateway.selectorLabels" . | nindent 4 }}
