apiVersion: v1
kind: Service
metadata:
  name: {{ include "chatops-engine.fullname" . }}
  labels:
    {{- include "chatops-engine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chatops-engine.selectorLabels" . | nindent 4 }}
